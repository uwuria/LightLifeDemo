using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pruebauwuria : MonoBehaviour
{
    // Start is called before the first frame updategg
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
